# Agent skill bundles vendored into the workspace — tooling, not part of the site.
.agents/
.claude/

# Client requirement PDFs. This repo is PUBLIC — kept out on purpose.
requirment/

# QA output (tools/qa.js writes screenshot dirs)
qa-shots/
shots-*/
rm.png

# Build staging
_site/

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Higgsfield job scratch: response metadata + expiring CDN URLs, not source.
# The still PROMPTS beside them are kept — they document how each opener was made.
video-brief*/**/*.json
video-brief*/**/*.err
